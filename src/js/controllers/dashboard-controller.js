import { renderDashboard } from "../ui/dashboard-view.js"; export class DashboardController { constructor({ service, content }) { this.service = service; this.content = content; } render() { this.content.innerHTML = renderDashboard(this.service.getData()); this.content.querySelectorAll("[data-target]").forEach((card) => card.addEventListener("click", () => { window.location.hash = card.dataset.target; })); } }
