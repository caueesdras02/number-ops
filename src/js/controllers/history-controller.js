import {renderHistory} from '../ui/history-view.js';export class HistoryController{constructor({service,numbers,content}){this.service=service;this.numbers=numbers;this.content=content;this.filters={}}render(){let e=this.service.list(this.filters.numberId);if(this.filters.type)e=e.filter(x=>x.type.includes(this.filters.type.toUpperCase()));this.content.innerHTML=renderHistory(e,this.numbers.state.numbers,this.filters);this.content.querySelectorAll('[data-filter]').forEach(x=>x.addEventListener('input',()=>{this.filters[x.dataset.filter]=x.value;this.render()}))}}
