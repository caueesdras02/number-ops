export function renderDirectory(type, items) { const label={clients:'Clientes',groups:'Grupos',responsibles:'Colaboradores'}[type]; return `<section><div class="page-heading"><h2>${label}</h2><button class="button button-primary" data-action="add">Adicionar</button></div><div class="table-card"><table><tbody>${items.map(i=>`<tr><td><strong>${i.name}</strong>${type==='responsibles'?`<span class="table-secondary">${i.team||'Sem equipe'}</span>`:''}</td><td>${i.isActive?'Ativo':'Arquivado'}</td><td class="table-actions"><button class="button button-quiet" data-action="edit" data-id="${i.id}">Editar</button>${i.isActive?`<button class="button button-danger" data-action="archive" data-id="${i.id}">Arquivar</button>`:`<button class="button button-primary" data-action="restore" data-id="${i.id}">Desarquivar</button>`}</td></tr>`).join('')||'<tr><td>Nenhum registro.</td></tr>'}</tbody></table></div></section>`; }
export function renderDirectoryForm(type,item={}) { return `<div class="modal-backdrop"><form id="directory-form" class="modal-card" data-id="${item.id||''}"><div class="modal-header"><h2>${item.id?'Editar':'Adicionar'} ${type==='responsibles'?'Colaborador':type==='clients'?'Cliente':'Grupo'}</h2><button type="button" class="icon-button" data-action="close-form">×</button></div><label>Nome<input class="input" name="name" required value="${item.name||''}"></label>${type==='responsibles'?'<label>Equipe / squad<input class="input" name="team" value="'+(item.team||'')+'"></label>':''}<div class="form-actions"><button type="button" class="button button-quiet" data-action="close-form">Cancelar</button><button class="button button-primary">Salvar</button></div></form></div>`; }
